<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->

    </style>

    <style name="InputLabel" parent="TextAppearance.AppCompat.Caption">
        <item name="android:paddingBottom">@dimen/input_label_vertical_spacing</item>
        <item name="android:paddingLeft">@dimen/input_label_horizontal_spacing</item>
        <item name="android:paddingRight">@dimen/input_label_horizontal_spacing</item>
    </style>

    <style name="TextAppearence.App.TextInputLayout" parent="@android:style/TextAppearance">
    <item name="android:textColor">@color/blue</item>
    </style>

    <style name="TextAppearence.App.TextInputLayout2" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/green</item>
    </style>

    <style name="DialogTheme">
                <item name="android:windowEnterAnimation">@animator/slide_left</item>
                <item name="android:windowExitAnimation">@animator/slide_right</item>
    </style>
</resources>
