<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    >
    <TextView
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/exerciseName"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:textSize="18sp" />
    <ListView
        android:layout_marginLeft="15dp"
        android:layout_marginTop="15dp"
        android:layout_marginRight="15dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/exerciseName"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:id="@+id/workoutListView">
    </ListView>
    <Space
        android:layout_width="1dp"
        android:layout_height="15dp"
        android:layout_below="@id/workoutListView"
        android:id="@+id/exerciseTitleSpacer"
        />
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Set"
        android:id="@+id/addSetButton"
        android:layout_below="@id/exerciseTitleSpacer"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="@color/green"
        />


    <Space
        android:layout_width="1dp"
        android:layout_height="15dp"
        android:layout_below="@id/addSetButton"
        android:id="@+id/exerciseTitleSpacer2"
        />
</LinearLayout>