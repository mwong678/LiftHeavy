<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">




    <TextView
        android:layout_width="fill_parent"
        android:gravity="center"
        android:background="@color/white"
        android:layout_height="wrap_content"
        android:text=""
        android:id="@+id/startWorkoutTextView"
        android:layout_centerHorizontal="true"
        android:textColor="@color/green"
        android:textSize="18sp" />

    <Space
        android:layout_width="1dp"
        android:layout_height="10dp"
        android:layout_below="@id/startWorkoutTextView"
        android:id="@+id/workoutTitleSpacer"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:id="@+id/cancelWorkoutButton"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"

        />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Exercises"
        android:id="@+id/addExercisesButton"
        android:layout_alignParentBottom="true"
        android:layout_alignTop="@id/cancelWorkoutButton"
        android:layout_toLeftOf="@id/cancelWorkoutButton"
        android:layout_toStartOf="@id/cancelWorkoutButton"
        />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/workoutTitleSpacer"
        android:layout_above="@id/addExercisesButton"
        android:id="@+id/startWorkoutListView">

    </ListView>



</RelativeLayout>